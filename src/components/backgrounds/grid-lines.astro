<div class="cutting-mat" aria-hidden="true"></div>

<style>
	.cutting-mat {
		position: fixed;
		inset: 0;
		z-index: -1;
		background-color: var(--mat);
		background-image:
			linear-gradient(var(--mat-grid) 1px, transparent 1px),
			linear-gradient(90deg, var(--mat-grid) 1px, transparent 1px),
			radial-gradient(circle at 50% 35%, rgb(255 255 255 / 5%), transparent 68%);
		background-size:
			24px 24px,
			24px 24px,
			100% 100%;
	}

	.cutting-mat::after {
		position: absolute;
		inset: 8px;
		border: 1px solid rgb(255 255 255 / 8%);
		background:
			repeating-linear-gradient(90deg, var(--mat-tick) 0 1px, transparent 1px 12px) top / 100% 4px
				no-repeat,
			repeating-linear-gradient(90deg, var(--mat-tick) 0 1px, transparent 1px 12px) bottom / 100%
				4px no-repeat,
			repeating-linear-gradient(0deg, var(--mat-tick) 0 1px, transparent 1px 12px) left / 4px 100%
				no-repeat,
			repeating-linear-gradient(0deg, var(--mat-tick) 0 1px, transparent 1px 12px) right / 4px 100%
				no-repeat;
		content: '';
		pointer-events: none;
	}
</style>
