---
import { cn } from '@/lib/utils';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<h4 class={cn('font-serif text-xl leading-tight font-medium tracking-[-0.015em]', className)}>
	<slot />
</h4>
