---
import { cn } from '@/lib/utils';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<h2
	class={cn(
		'font-serif text-2xl leading-tight font-medium tracking-[-0.02em] sm:text-3xl',
		className
	)}
>
	<slot />
</h2>
